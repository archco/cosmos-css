button(class='btn info' id='btn-toast') Toast

script.
  var toastCount = 0;
  window.addEventListener('load', function () {
    document.querySelector('#btn-toast').addEventListener('click', function () {
      toastCount++;
      Cosmos.Toast.makeText('toast test ' + toastCount).show();
    });
  });
